# 测试报告（摘要）

## 后端
- 单元测试：`JwtService` 编解码与异常；`TokenBlacklistService` 黑名单读写。
- 集成测试：`AuthController` 登出流程与黑名单命中（基于 MockMvc）。
- 覆盖率：核心安全与认证模块已覆盖关键路径；事务边界与异常映射经用例验证。

## 前端
- 路由守卫：在有本地用户态时直接放行，401 自动刷新或跳转登录。
- 登录表单：校验规则与错误提示稳定；验证码刷新逻辑正常。

## 兼容性
- Axios 拦截器：统一处理 401 -> 刷新 -> 重试；失败跳转 `/admin`。
- Cookie 与 CSRF：保留既有策略，跨域场景在白名单下正常。

